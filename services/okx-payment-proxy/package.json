{
  "name": "@ai-arena/okx-payment-proxy",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "x402 v2 payment gate in front of POST /v1/okx/create-agent. Decodes EIP-3009 transferWithAuthorization from the X-PAYMENT header, settles on X Layer (eip155:196), then forwards to agent-service.",
  "scripts": {
    "dev": "node --import tsx src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0"
  }
}
